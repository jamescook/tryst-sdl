# tryst-sdl: SDL3 rendering, audio and input for tryst.
#
# The shard entry point, so `require "tryst-sdl"` pulls in the lot. The
# real content lives under src/tryst/sdl/*.cr, one file per concept -
# mirroring tryst's own src/tryst/*.cr layout.
require "./tryst/sdl"
